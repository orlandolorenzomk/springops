package org.kreyzon.springops.core.email.dto;

import java.util.UUID;

/**
 * Interface representing a Data Transfer Object (DTO) for email configuration.
 * This interface defines the contract for email configuration DTOs, which should
 * include an identifier.
 *
 * @author Domenico Ferraro
 */
public interface EmailConfigurationDto {

    String getId();
}
